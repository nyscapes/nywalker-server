{{outlet instances=sortedInstances}}

{{#bs-accordion as |acc|}}
  {{#each sortedInstances as |instance|}}
    {{#acc.item value=instance.id title=instance.title}}
      {{instance-listing instance=instance}}
    {{/acc.item}}
  {{/each}}
{{/bs-accordion}}
