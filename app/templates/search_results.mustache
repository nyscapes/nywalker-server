<h3>Results for “{{search_term}}”</h3>
{{#results_table}}
  <table class="table table-striped table-hover">
    <thead>
      <tr>
        <th>&nbsp</th>
        <th>Name (lat, lon)</th>
      </tr>
    </thead>
    <tbody>
      {{#results}}
        <tr>
          <td>
            <button class="button btn-primary btn-xs">Map!</button>
          </td>
          <td>
            {{name}} <small>({{lat}}, {{lon}})</small><br />
            <small>{{description}}</small>
          </td>
        </tr>
      {{/results}}
    </tbody>
  </table>
  <div id="resultsMap">This is where the map will go ha ha.</div>
{{/results_table}}
{{^results_table}}
  <p>No results found! Try <a href="https://en.wikipedia.org/w/index.php?search={{search_term}}">Searching Wikipedia</a> and entering the data manually.</p>
{{/results_table}}

