      <div id="map">
      </div>
      <script>
        var map = L.map('map').setView([0,0], 1);
        var baseLayer = L.tileLayer.provider('OpenStreetMap.Mapnik').addTo(map);
        var dots = L.markerClusterGroup();
        {{#places}}
        dots.addLayer(L.circleMarker([{{lat}}, {{lon}}]).bindPopup("<h4><a href='/places/{{slug}}'>{{name}}</a></h4><small><strong>Referred to as:</strong><br/>{{{place_names}}}<strong>Mentions:</strong> {{count}}</small>"));
        {{/places}}
        dots.addTo(map);
        map.fitBounds(dots.getBounds());
      </script>
