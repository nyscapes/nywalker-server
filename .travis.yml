language: ruby
rvm:
  - 2.2
  - 2.3
services:
    - postgresql
before_script:
    - psql -U postgres -c "create extension postgis"
    - psql -c 'create database travis_ci_test;' -U postgres
    - rake db:migrate
